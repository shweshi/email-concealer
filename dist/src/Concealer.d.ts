export default class Concealer {
    private readonly dictionary;
    private domain;
    constructor();
    conceal: (string: string, domain?: string) => string;
    private extractEmails;
    private fill;
    private add;
    private addOrUpdateIncrement;
}
